#include <iostream>
#include <vector>

using namespace std;
using ll = long long;
using ii = pair<ll, ll>;
using vi = vector<ll>;

int main()
{
    ll x = 2, y = 3;
    ii z(x, y);
    vi ns { x, y, 10, -1 };

    return 0;
}
